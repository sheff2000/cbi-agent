# убрать name === '...' из wsClient

# отвязать sendMsgService от services.get(...) - подписать wsAgent/metrika на событие с лать сразу с него

# ввести простое состояние metrikaRunning чтоб не кидать его в stop когда он не запущен
# перестать логать default-пакеты в metrika